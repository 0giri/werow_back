spring:
  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
#    url: jdbc:mysql://localhost:3306/werow?useSSL=false&characterEncoding=utf8&serverTimezone=Asia/Seoul&allowPublicKeyRetrieval=true
    url: jdbc:mysql://${DB_HOST:localhost}:3306/werow?useSSL=false&characterEncoding=utf8&serverTimezone=Asia/Seoul&allowPublicKeyRetrieval=true
    username: giri
    password: 9463
  jpa:
    hibernate:
#      ddl-auto: create-drop # 배포시 주석
      ddl-auto: ${DDL_AUTO:create-drop}
    properties:
      hibernate:
#        show_sql: true
        format_sql: true
  sql:
    init:
      mode: never

logging:
  level:
    org.hibernate.SQL: ${VIEW_SQL:debug} # 배포시 주석
    com.werow.web : ${VIEW_DEBUG:debug} # 배포시 주석
#    org.hibernate.type: trace

server:
  port: 8080
