spring:
  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/werow?useSSL=false&characterEncoding=utf8&serverTimezone=Asia/Seoul&allowPublicKeyRetrieval=true
    username: root
    password: 9463
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
#        use-new-id-generator-mappings: false
#        show_sql: true
        format_sql: true
  sql:
    init:
      mode: never

logging:
  level:
    org.hibernate.SQL: debug
#    org.hibernate.type: trace

server:
  port: 8080
